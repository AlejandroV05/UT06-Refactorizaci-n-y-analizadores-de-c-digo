namespace Renombrar
{
    internal class Program
    {
        //Renombrar
        //Cambiamos el nombre de un paquete, clase, método o campo, por un nombre más significativo.

        /*Código antiguo
         * Como se puede ver, no se especifica su función
         * 
         * public int metodo(int a, int b)
         * {
         *  return a*b
         * }
         */

        //Aplicando método

        //Como podemos ver, la función calculaba el area de un rectángulo
        public int AreaRectangulo(int a, int b)
        {
            return a * b;
        }
    }
}
